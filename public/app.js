const tg=window.Telegram?.WebApp;
const $=id=>document.getElementById(id);
const q=(selector,root=document)=>root.querySelector(selector);
const qa=(selector,root=document)=>[...root.querySelectorAll(selector)];
const esc=value=>String(value??'').replace(/[&<>'"]/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[char]));
const fmt=value=>new Intl.NumberFormat('en-US',{maximumFractionDigits:0}).format(Number(value||0));
const money=value=>{const n=Number(value||0);return n>=1e6?'$'+(n/1e6).toFixed(1)+'M':n>=1e3?'$'+(n/1e3).toFixed(n>=1e5?0:1)+'K':'$'+fmt(n)};
const pct=value=>(Number(value||0)>=0?'+':'')+Number(value||0).toFixed(1)+'%';
const duration=ms=>{const seconds=Math.max(0,Math.ceil(ms/1000));if(seconds>=3600)return Math.floor(seconds/3600)+'h '+Math.floor(seconds%3600/60)+'m';return Math.floor(seconds/60)+':'+String(seconds%60).padStart(2,'0')};
const storageKey='xradar_lab_operator_v1';
const deviceKey='xradar_lab_device_v1';
const defaultEditor={callSign:'Operator',gender:'custom',face:3,build:3,hair:4,gear:2};
function loadEditor(){try{return {...defaultEditor,...JSON.parse(localStorage.getItem(storageKey)||'{}')}}catch{return {...defaultEditor}}}
function deviceId(){let value=localStorage.getItem(deviceKey);if(!value){value=crypto.randomUUID?.()||String(Date.now())+'-'+Math.random().toString(36).slice(2);localStorage.setItem(deviceKey,value)}return value}

const state={game:null,config:null,tab:'lab',view:'lab',signalId:null,factors:new Set(),lastResult:null,lastExpected:new Set(),poll:null,toast:null,editor:loadEditor(),editorDirty:false,sound:localStorage.getItem('xradar_lab_sound')==='1',audio:null,hum:null,leaderboard:null,catalog:null,ton:null};
const UNIT_SPOTS={center:{x:43,y:65},terminal:{x:24,y:65},analyzer:{x:55,y:65},generator:{x:50,y:91},locker:{x:16,y:65},supply:{x:14,y:33},elevator:{x:90,y:65},workbench:{x:76,y:65},power:{x:52,y:91}};
const FLOOR_SPOTS=[{x:24,y:26},{x:65,y:26},{x:24,y:45},{x:65,y:45},{x:24,y:64},{x:65,y:64},{x:24,y:83},{x:65,y:83}];
const ROOM_POS={lab:[3,9],power:[44,9],workshop:[3,31],comms:[44,31],automation:[3,52],antenna:[44,52],analysis:[3,73],interceptor:[44,73]};
const ROOM={
  lab:{name:'Command Lab',desc:'The intelligence terminal and central operations floor.',effect:'Produces Intel. Higher levels sharply increase hourly output.'},
  power:{name:'Power Room',desc:'The station grid, reserve cells and emergency machinery.',effect:'+25 maximum Power and faster regeneration per level.'},
  workshop:{name:'Workshop & Storage',desc:'Engineering tools, recovered parts and protected storage.',effect:'Extends offline storage and discounts late upgrades.'},
  comms:{name:'Communications Hub',desc:'A low-latency backbone for every station system.',effect:'Reduces all construction time by 4% per level.'},
  automation:{name:'Automation Servers',desc:'Autonomous market monitoring while the operator is away.',effect:'+15% offline Intel production per level.'},
  antenna:{name:'Signal Array',desc:'Receives larger batches of external market observations.',effect:'Adds more signals to each intelligence wave.'},
  analysis:{name:'Risk Analysis Center',desc:'Reveals risk bands, mint authority and holder concentration.',effect:'More evidence becomes visible at levels 3, 6 and 9.'},
  interceptor:{name:'Wallet Interceptor',desc:'Observes coordinated activity from large wallets.',effect:'Shows smart-wallet activity and enables rare Part finds.'}
};
const OBJECT={terminal:['Command Terminal','Processes market intelligence and produces Intel.'],analyzer:['Signal Radar','Opens intercepted assets for evidence-based risk assessment.'],generator:['Power Control','Maintains the underground station power grid.'],locker:['Equipment Locker','Stores visible operator clothing and tools.'],supply:['Surface Supply Access','Receives 2–4 construction Parts every six hours.'],elevator:['Expansion Elevator','Opens the full station view and every buildable room.']};
const ACTION={emergency_lights:['Restore emergency lights','Bring the Command Lab back online.'],boot_terminal:['Boot the terminal','Start the intelligence workstation and load the first signal.'],repair_power:['Stabilize the grid','Repair the damaged Power Control system.'],daily_supply:['Collect shipment','Recover the waiting Parts shipment.'],terminal_sync:['Synchronize Intel','Process a fresh station intelligence batch.'],generator_charge:['Recharge reserve','Restore part of the station Power reserve.']};
const ITEM={field_coat:['Field Operations Coat','Standard protection for underground work.'],insulated_gloves:['Insulated Gloves','Work actions complete 5% faster.'],analyst_goggles:['Analyst Optics','Adds a visible analysis aid to the operator.'],utility_vest:['Utility Harness','Late upgrades cost fewer Parts.'],field_tablet:['Field Tablet','Adds one extra intercepted signal.'],headlamp:['Industrial Headlamp','Work on new levels completes 5% faster.']};
const TASK={lights:['Restore the lights','Activate emergency lighting at Power Control.'],terminal:['Bring the terminal online','Boot the Command Terminal.'],first_signal:['Assess the first signal','Inspect the evidence and submit a conclusion.'],repair:['Stabilize station power','Repair Power Control.'],power_floor:['Open the Power Room','Use the elevator to expand the Lab.'],supply:['Collect the supply drop','Recover Parts from the surface access.'],recon:['Assess a market signal','Review the intercepted evidence.'],terminal_sync:['Synchronize intelligence','Process a fresh batch at the terminal.'],active_job:['Operation in progress','The assigned operator is completing the current task.']};
const HAIR=['#17191d','#4a3027','#76513b','#a8b4b5','#d6cfc0','#7c2223','#31555d','#c1c8cf'];
const GEAR=['#35d0ba','#5fd4ff','#e8b339','#9dacc0','#d5efe9'];

async function api(url,options={}){
  const response=await fetch(url,{method:options.method||'GET',headers:options.body?{'Content-Type':'application/json'}:undefined,body:options.body?JSON.stringify(options.body):undefined});
  const data=await response.json().catch(()=>({}));
  if(!response.ok){const error=new Error(englishError(data.error,data.message));error.status=response.status;error.code=data.error;throw error}
  return data;
}
function englishError(code,fallback){return ({HERO_BUSY:'The operator is already assigned.',ROOM_BUSY:'That room is already under construction.',NOT_ENOUGH_DATA:'Not enough Intel.',NOT_ENOUGH_COMPONENTS:'Not enough Parts.',NOT_ENOUGH_ENERGY:'Not enough Power.',ACTION_LOCKED:'That operation is not available yet.',LOCKED_ROOM:'This room is still locked.',MAX_LEVEL:'This room has reached level 10.',UNKNOWN_SIGNAL:'This signal is no longer available.',ITEM_LOCKED:'This item has not been recovered yet.',INCIDENT_LOCKED:'Complete the restoration protocol first.',INCIDENT_ACTIVE:'Contain the active station incident first.',INCIDENT_COOLDOWN:'Security systems are still analyzing the last incident.',NO_ACTIVE_INCIDENT:'The incident has already been contained.',INVALID_APPEARANCE:'Check the operator profile values.',INVALID_COSMETIC:'Unknown station appearance.',COSMETIC_LOCKED:'This appearance is part of the cosmetic station pack.',REFERRAL_EXISTS:'A referral is already connected.',SELF_REFERRAL:'You cannot use your own referral code.',REFERRAL_NOT_FOUND:'Referral code not found.',XRADAR_NOT_CONFIGURED:'Live XRadar waves are not configured yet.',STARS_NOT_CONFIGURED:'Telegram Stars are not configured yet.',TON_NOT_CONFIGURED:'TON payments are not configured yet.'})[code]||fallback||'The operation could not be completed.'}
function haptic(type='light'){try{if(!tg?.isVersionAtLeast?.('6.1'))return;if(type==='success')tg.HapticFeedback?.notificationOccurred('success');else if(type==='error')tg.HapticFeedback?.notificationOccurred('error');else tg.HapticFeedback?.impactOccurred(type)}catch{}}
function audioContext(){state.audio||=new(window.AudioContext||window.webkitAudioContext)();state.audio.resume?.();return state.audio}
function tone(freq=520,length=.06){if(!state.sound)return;try{const ctx=audioContext(),osc=ctx.createOscillator(),gain=ctx.createGain();osc.frequency.value=freq;osc.type='sine';gain.gain.setValueAtTime(.025,ctx.currentTime);gain.gain.exponentialRampToValueAtTime(.0001,ctx.currentTime+length);osc.connect(gain).connect(ctx.destination);osc.start();osc.stop(ctx.currentTime+length)}catch{}}
function setAmbient(on){state.sound=on;localStorage.setItem('xradar_lab_sound',on?'1':'0');$('soundButton').classList.toggle('on',on);$('soundButton').textContent=on?'◉':'◒';if(on&&!state.hum){try{const ctx=audioContext(),osc=ctx.createOscillator(),gain=ctx.createGain(),filter=ctx.createBiquadFilter();osc.type='sawtooth';osc.frequency.value=42;filter.type='lowpass';filter.frequency.value=120;gain.gain.value=.008;osc.connect(filter).connect(gain).connect(ctx.destination);osc.start();state.hum={osc,gain}}catch{}}if(state.hum)state.hum.gain.gain.setTargetAtTime(on ? .008 : 0,state.audio.currentTime,.15)}
function notify(message,error=false){clearTimeout(state.toast);$('toast').textContent=message;$('toast').classList.toggle('error',error);$('toast').classList.add('show');state.toast=setTimeout(()=>$('toast').classList.remove('show'),error?4300:3000)}
function taskCopy(task){const fixed=TASK[task.id];if(fixed)return{title:fixed[0],desc:fixed[1]};if(task.id?.startsWith('build_')){const room=ROOM[task.id.slice(6)];return{title:'Build: '+(room?.name||'new room'),desc:room?.desc||'Expand the underground station.'}}return{title:task.title||'Station directive',desc:task.description||'Complete the current operation.'}}
function actionCopy(action){return ACTION[action?.id]||[action?.label||'Start operation',action?.description||'Assign the operator to this task.']}

function setGame(game){
  const previous=state.game,oldJob=previous?.hero?.job?.id;state.game=game;
  if(!state.editorDirty&&game.profile?.appearance){state.editor={...defaultEditor,...game.profile.appearance};localStorage.setItem(storageKey,JSON.stringify(state.editor))}
  if(oldJob&&!game.hero.job){notify('Operation complete.');tone(760,.12);haptic('success')}
  if(previous){const delta=game.resources.data-previous.resources.data;if(delta>=1)resourceFly('+'+fmt(delta)+' Intel');const parts=game.resources.components-previous.resources.components;if(parts>=1)resourceFly('+'+fmt(parts)+' Parts')}
  renderAll();schedulePoll();
}
function schedulePoll(){clearTimeout(state.poll);const busy=state.game?.hero?.job||state.game?.progression?.secondaryJob;state.poll=setTimeout(()=>refresh().catch(()=>{}),busy?900:20000)}
async function refresh(){const response=await api('/api/game');setGame(response.game)}
function renderAll(){if(!state.game)return;applyTheme();renderHud();renderLab();renderCrew();renderSignals();renderMissions();renderStorage();renderReturn()}
function applyTheme(){const cosmetic=state.game.profile.cosmetics||{};$('game').dataset.neon=cosmetic.neon||'cyan';$('game').dataset.floor=cosmetic.floor||'steel';$('game').dataset.skin=cosmetic.heroSkin||'standard'}
function renderHud(){const game=state.game;$('intelValue').textContent=fmt(game.resources.data);$('powerValue').textContent=fmt(game.resources.energy)+'/'+fmt(game.resources.energyMax);$('partsValue').textContent=fmt(game.resources.components);$('levelValue').textContent=game.hero.level;$('xpBar').style.width=(game.hero.xp%100)+'%'}
function renderLab(){
  const game=state.game,task=game.recommendedTask,copy=task?taskCopy(task):{title:'All systems stable'};
  $('stationStatus').textContent=game.progression.onboarding.completed?'Online · '+fmt(game.resources.productionPerHour)+' Intel/h · '+game.resources.offlineCapacityHours+'h storage':'Emergency restoration protocol';
  $('goalText').textContent=copy.title;
  const job=game.hero.job,second=game.progression.secondaryJob,displayJob=job||second;$('jobStrip').classList.toggle('hidden',!displayJob);$('goalButton').classList.toggle('hidden',Boolean(displayJob));
  if(displayJob){$('jobTitle').textContent=jobTitle(displayJob);$('jobTime').textContent=duration(displayJob.remainingMs);$('jobBar').style.width=Math.max(2,100-(displayJob.remainingMs/(displayJob.durationMs||1))*100)+'%'}
  $('secondaryJob').classList.toggle('hidden',!job||!second);if(job&&second)$('secondaryJob').textContent='Automation slot · '+jobTitle(second)+' · '+duration(second.remainingMs);
  for(const object of game.objects){const element=q('[data-object="'+object.id+'"]');if(!element)continue;element.classList.toggle('ready',Boolean(object.action?.enabled||object.action?.type==='navigate'));element.classList.toggle('attn',Boolean(object.action&&object.id==='generator'&&!game.progression.onboarding.completed))}
  const incident=game.progression.incidents?.active;q('.scene').classList.toggle('incident',Boolean(incident));$('incidentButton').title=incident?'Active: '+incident.title:game.progression.incidents?.ready?'Run tactical incident':'Security systems standing by';
  renderRoomMap();renderUnits();
}
function renderRoomMap(){
  const game=state.game;
  $('roomMap').innerHTML=game.roomOrder.map(id=>{const room=game.rooms[id],position=ROOM_POS[id],copy=ROOM[id];const status=room.construction?'building':room.level?'open':room.unlocked?'ready':'locked';const progress=room.construction?Math.round(room.construction.progress*100):0;return `<button class="room-node ${status}" style="left:${position[0]}%;top:${position[1]}%;width:38%;height:18%;--progress:${progress}%" data-room="${id}" type="button"><b>${room.level?'LV '+room.level:room.unlocked?'BUILD':'LOCKED'}</b><span>${esc(copy.name)}</span></button>`}).join('');
}
function jobTitle(job){if(job.type==='construction')return (job.targetLevel===1?'Opening ':'Upgrading ')+(ROOM[job.roomId]?.name||'room');return ACTION[job.actionId]?.[0]||'Station operation'}
function spotForNode(node){const id=String(node||'');if(state.view==='overview')return FLOOR_SPOTS[Math.min(7,state.game.hero.floor||0)];if(id.includes('terminal'))return UNIT_SPOTS.terminal;if(id.includes('analyzer'))return UNIT_SPOTS.analyzer;if(id.includes('generator'))return UNIT_SPOTS.generator;if(id.includes('locker'))return UNIT_SPOTS.locker;if(id.includes('supply'))return UNIT_SPOTS.supply;if(id.includes('elevator'))return id.startsWith('floor_')?UNIT_SPOTS.power:UNIT_SPOTS.elevator;if(id.startsWith('floor_'))return UNIT_SPOTS.power;return UNIT_SPOTS.center}
function setUnit(unit,spot,mode='idle',baseFacesRight=true){const oldX=Number(unit.dataset.x||spot.x),moved=Math.abs(oldX-spot.x)>.5||Math.abs(Number(unit.dataset.y||spot.y)-spot.y)>.5;if(moved){const direction=spot.x>=oldX?1:-1;unit.style.setProperty('--flip',baseFacesRight?direction:-direction)}unit.style.left=spot.x+'%';unit.style.top=spot.y+'%';unit.dataset.x=spot.x;unit.dataset.y=spot.y;clearTimeout(unit._poseTimer);unit.classList.remove('idle','walking','working','alarm','tired');if(moved){unit.classList.add('walking');unit._poseTimer=setTimeout(()=>{unit.classList.remove('walking');unit.classList.add(mode)},880)}else unit.classList.add(mode)}
function appearanceStyle(element){const editor=state.editor;element.dataset.gender=editor.gender||'custom';element.style.setProperty('--body-scale',(0.93+Number(editor.build||3)*.018).toFixed(3));element.style.setProperty('--face-scale',(0.955+Number(editor.face||3)*.012).toFixed(3));element.style.setProperty('--hair-color',HAIR[(Number(editor.hair||4)-1)%HAIR.length]);element.style.setProperty('--gear-color',GEAR[(Number(editor.gear||2)-1)%GEAR.length])}
function equipmentClasses(element){const outfit=state.game.hero.outfit||{};for(const id of Object.keys(ITEM))element.classList.toggle('has-'+id,Object.values(outfit).includes(id));element.classList.remove('rank-1','rank-2','rank-3');element.classList.add('rank-'+Math.min(3,Math.floor((state.game.hero.level-1)/5)+1))}
function renderUnits(){
  const game=state.game,operator=$('operatorUnit'),engineer=$('engineerUnit'),job=game.hero.job,incident=Boolean(game.progression.incidents?.active);appearanceStyle(operator);equipmentClasses(operator);$('operatorUnitLabel').textContent=state.editor.callSign||'Operator';
  const mode=incident?'alarm':game.resources.energy<=0?'tired':job?'working':'idle';setUnit(operator,spotForNode(game.hero.node),mode,true);
  const engineerOpen=Boolean(game.crew?.engineer?.recruited);engineer.classList.toggle('hidden',!engineerOpen);if(engineerOpen){const supports=job&&(job.type==='construction'||['repair_power','generator_charge'].includes(job.actionId));const spot=state.view==='overview'?FLOOR_SPOTS[1]:supports?UNIT_SPOTS.power:UNIT_SPOTS.workbench;setUnit(engineer,spot,incident?'alarm':supports?'working':'idle',false)}
}
function moveOperatorToObject(id){if(state.view!=='lab')setView('lab');setUnit($('operatorUnit'),UNIT_SPOTS[id]||UNIT_SPOTS.center,state.game.progression.incidents?.active?'alarm':'idle',true)}
function renderCrew(){
  const game=state.game,editor=state.editor;$('operatorName').textContent=editor.callSign||game.profile.firstName||'Operator';$('callSign').value=editor.callSign||'Operator';qa('button[data-gender]').forEach(button=>button.classList.toggle('active',button.dataset.gender===editor.gender));qa('[data-editor-range]').forEach(input=>input.value=editor[input.dataset.editorRange]||input.value);appearanceStyle($('operatorVisual'));equipmentClasses($('operatorVisual'));
  const characteristics=game.hero.characteristics;$('crewStats').innerHTML=`<div class="stat"><b>+${characteristics.workSpeedPct}%</b><span>Work speed</span></div><div class="stat"><b>${characteristics.energyCapacity}</b><span>Power capacity</span></div><div class="stat"><b>LV ${characteristics.analysisLevel}</b><span>Analysis</span></div>`;
  $('crewList').innerHTML=Object.values(game.crew).map(member=>`<div class="crew-row" style="${member.recruited?'':'opacity:.4'}"><div class="mini-avatar">${member.id.slice(0,2).toUpperCase()}</div><div class="row-copy"><b>${esc(member.name)}</b><span>${esc(member.recruited?member.role:member.id==='engineer'?'Recruit after opening the Power Room':'Recruit after opening the Signal Array')}</span></div><span class="row-end">${esc(member.status)}</span></div>`).join('');
  renderCosmetics();renderReferral();
}
function renderCosmetics(){const game=state.game,current=game.profile.cosmetics,owned=new Set(game.progression.commerce.entitlements);const options={neon:[['cyan','Cyan'],['amber','Amber'],['ice','Ice']],floor:[['steel','Steel'],['concrete','Concrete'],['grid','Grid']],heroSkin:[['standard','Standard'],['field','Field'],['command','Command']]};$('cosmeticList').innerHTML=Object.entries(options).map(([key,values])=>`<div class="option-row"><div class="row-copy"><b>${key==='heroSkin'?'Operator suit':key[0].toUpperCase()+key.slice(1)}</b><span>Purely visual. No gameplay advantage.</span></div>${values.map(([value,label],index)=>{const unlocked=index===0||owned.has('cosmetic:'+key+':'+value);return `<button class="${current[key]===value?'active':''}" data-cosmetic-key="${key}" data-cosmetic-value="${value}" type="button" ${unlocked?'':'disabled'}>${label}</button>`}).join('')}</div>`).join('')}
function renderReferral(){const profile=state.game.profile,referrals=state.game.progression.referrals;$('referralCard').innerHTML=`<div class="card-top"><div><h3>Your code: ${esc(profile.referralCode)}</h3><p>A referral qualifies only after operator level 3 and one completed signal. Registration alone gives nothing.</p></div><span class="tag">${referrals.total} qualified</span></div><div class="field" style="margin-top:10px"><label>Connect inviter code</label><input id="referralInput" type="text" maxlength="24" placeholder="XR…" ${profile.referralConnected?'disabled':''}></div><button class="secondary full" data-referral-connect type="button" ${profile.referralConnected?'disabled':''}>${profile.referralConnected?'Referral connected':'Connect code'}</button>`}

function renderSignals(){
  const recon=state.game.progression.recon,body=$('signalsBody');if(state.lastResult){body.innerHTML=resultMarkup(state.lastResult);return}const selected=recon.signals.find(signal=>signal.id===state.signalId);if(selected){body.innerHTML=signalDetail(selected);requestAnimationFrame(()=>drawChart(selected.market));return}
  if(!recon.unlocked||recon.requiresAntenna){body.innerHTML='<div class="card amber"><div class="card-top"><div><h3>Signal Array offline</h3><p>Build the Signal Array to receive recurring market observations. The tutorial signal remains available before that.</p></div><span class="tag locked">Locked</span></div><div class="row-actions"><button class="secondary" data-room-focus="antenna" type="button">Show required room</button></div></div>';return}
  if(!recon.signals.length){body.innerHTML='<div class="card"><h3>Scanning external feeds</h3><p>The station is preparing the next intelligence batch.</p></div>';return}
  body.innerHTML=`<div class="card"><div class="card-top"><div><h3>Intercepted assets</h3><p>${recon.signals.length} signals are ready. Names remain hidden until an outcome is verified.</p></div><span class="tag">${recon.signals.length} live</span></div><div class="signal-list">${recon.signals.map(signalRow).join('')}</div>${state.config?.liveWaves?'<button class="secondary full" data-sync-live type="button">Replace with live XRadar wave</button>':''}</div><div class="card accent"><h3>Assessment protocol</h3><p>Use liquidity, holder distribution and buying pressure. Advanced station rooms reveal additional evidence; they never choose for you.</p></div>`;requestAnimationFrame(()=>recon.signals.forEach(drawSpark))
}
function signalRow(signal){const market=signal.market||{},up=Number(market.change24h)>=0;return `<button class="signal" data-signal-open="${esc(signal.id)}" type="button"><canvas class="spark" data-spark="${esc(signal.id)}" width="140" height="60"></canvas><span><b>${esc(signal.name)}</b><small>${money(market.liquidityUsd)} liquidity · ${fmt(market.holders)} holders</small></span><span class="change ${up?'up':'down'}">${pct(market.change24h)}</span></button>`}
function signalDetail(signal){
  const market=signal.market||{},up=Number(market.change24h)>=0,hasConcentration=Number.isFinite(Number(signal.concentration)),hasMutable=typeof signal.mutable==='boolean',hasWallets=Number.isFinite(Number(signal.smartWallets));
  const factors=[['liquidity','Thin liquidity','Limited market depth may make an exit difficult.',true],['holders','Holder concentration','A few wallets can control price movement.',hasConcentration],['mutable','Mutable contract','The token supply rules may still be changed.',hasMutable],['pressure','Sell pressure','Selling activity currently exceeds buying.',true],['lp','Unlocked liquidity','The liquidity pool can potentially be removed.',hasMutable],['mint','Active mint authority','New token supply can still be issued.',hasMutable]];
  const metric=(label,value,bad,visible=true)=>visible?`<div class="metric ${bad?'bad':''}"><span>${label}</span><b>${value}</b></div>`:`<div class="metric locked"><span>${label}</span><b>Requires Analysis upgrade</b></div>`;
  return `<button class="signal-back" data-signal-back type="button">← All intercepted signals</button><div class="card accent"><div class="token-head"><div><div class="eyebrow">${signal.source==='xradar'?'Live XRadar signal':'Station simulation'}</div><h3>${esc(signal.name)}</h3></div><div class="token-price"><b class="${up?'up':'down'}">${pct(market.change24h)}</b><span>observed movement</span></div></div><canvas id="signalChart" class="chart" width="700" height="300"></canvas><div class="metrics">${metric('Liquidity',money(market.liquidityUsd),signal.liquidity<45)}${metric('Volume',money(market.volume24hUsd),false)}${metric('Top holder group',signal.concentration+'%',signal.concentration>60,hasConcentration)}${metric('Holders',fmt(market.holders),false)}${metric('Mint authority',market.mintRevoked?'Revoked':'Active',!market.mintRevoked,hasMutable)}${metric('Smart wallets',fmt(signal.smartWallets),false,hasWallets)}</div>${signal.riskBand?`<div class="chips"><span class="chip">Analysis hint: ${signal.riskBand.toUpperCase()} risk band</span></div>`:''}</div><div class="section-title">Mark visible warning signs</div>${factors.map(f=>f[3]?`<button class="factor ${state.factors.has(f[0])?'selected':''}" data-factor="${f[0]}" type="button"><i>✓</i><span><b>${f[1]}</b><span>${f[2]}</span></span></button>`:'').join('')}<div class="assessment"><button class="safe" data-assessment="study" type="button">Clear for research</button><button class="risky" data-assessment="skip" type="button">Flag as dangerous</button></div>`;
}
function expectedFactors(signal){const market=signal.market||{},factors=[];if(signal.liquidity<45)factors.push('liquidity');if(Number.isFinite(Number(signal.concentration))&&signal.concentration>60)factors.push('holders');if(signal.mutable===true)factors.push('mutable');if(Number(market.sells)>Number(market.buys))factors.push('pressure');if(typeof market.lpLocked==='boolean'&&!market.lpLocked)factors.push('lp');if(typeof market.mintRevoked==='boolean'&&!market.mintRevoked)factors.push('mint');return new Set(factors)}
function resultMarkup(result){const chosen=[...state.factors],expected=state.lastExpected,matched=chosen.filter(value=>expected.has(value)).length,missed=[...expected].filter(value=>!state.factors.has(value)).length;return `<div class="result ${result.correct?'':'bad'}"><b>${result.correct?'Assessment confirmed':'Assessment reviewed'}</b><p>${esc(result.explanation)} You identified ${matched} visible warning sign${matched===1?'':'s'}${missed?', with '+missed+' still unmarked':''}.</p></div><div class="card"><div class="stat-grid"><div class="stat"><b>${result.risk}</b><span>Risk score</span></div><div class="stat"><b>+${result.reward.data||0}</b><span>Intel</span></div><div class="stat"><b>+${(result.reward.components||0)+(result.reward.dailyComponents||0)}</b><span>Parts</span></div></div>${result.rareFind?'<p>Rare wallet-interceptor find: +1 Part.</p>':''}<button class="secondary full" data-result-close type="button">Return to signals</button>${state.game.progression.onboarding.completed?'<button class="primary live-scan" data-live-scan type="button">Open live XRadar terminal</button>':''}</div>`}
function drawSpark(signal){const canvas=q('[data-spark="'+CSS.escape(signal.id)+'"]');if(canvas)drawLine(canvas,signal.market?.priceSeries||[],Number(signal.market?.change24h)>=0?'#51d99b':'#e26e62',false)}
function drawChart(market){const canvas=$('signalChart');if(canvas)drawLine(canvas,market?.priceSeries||[],Number(market?.change24h)>=0?'#51d99b':'#e26e62',true)}
function drawLine(canvas,values,color,grid){if(!values.length)return;const ctx=canvas.getContext('2d'),width=canvas.width,height=canvas.height,pad=grid?30:4,min=Math.min(...values),max=Math.max(...values),range=max-min||1;ctx.clearRect(0,0,width,height);if(grid){ctx.strokeStyle='#183039';ctx.lineWidth=1;for(let i=1;i<4;i++){ctx.beginPath();ctx.moveTo(0,height*i/4);ctx.lineTo(width,height*i/4);ctx.stroke()}}ctx.beginPath();values.forEach((value,index)=>{const x=pad+(width-pad*2)*index/(values.length-1),y=pad+(height-pad*2)*(1-(value-min)/range);index?ctx.lineTo(x,y):ctx.moveTo(x,y)});ctx.strokeStyle=color;ctx.lineWidth=grid?5:3;ctx.lineJoin='round';ctx.stroke();if(grid){ctx.lineTo(width-pad,height-pad);ctx.lineTo(pad,height-pad);ctx.closePath();const gradient=ctx.createLinearGradient(0,0,0,height);gradient.addColorStop(0,color+'55');gradient.addColorStop(1,color+'00');ctx.fillStyle=gradient;ctx.fill()}}

function renderMissions(){
  const game=state.game;$('missionList').innerHTML=game.tasks.map(task=>{const copy=taskCopy(task);return `<div class="task-row"><div class="mini-avatar">${task.kind==='daily'?'24':task.kind==='recon'?'RX':'OP'}</div><div class="row-copy"><b>${esc(copy.title)}</b><span>${esc(copy.desc)}</span></div><button class="secondary" data-task="${esc(task.id)}" type="button">Open</button></div>`}).join('')||'<p>No urgent directives. Expand a room or assess a signal.</p>';
  const daily=game.progression.daily;$('dailyCard').innerHTML=`<div class="card-top"><div><h3>Five-signal daily assessment</h3><p>Complete five assessments. The challenge measures accuracy, not clicks.</p></div><span class="tag">${daily.attempts}/5</span></div><div class="progress"><i style="width:${Math.min(100,daily.attempts/5*100)}%"></i></div><div class="chips"><span class="chip">${daily.correct} correct</span><span class="chip gain">${daily.rewardClaimed?'Reward collected':'+5 Parts at 5 attempts'}</span></div>`;
  const season=game.progression.season;$('seasonCard').innerHTML=`<span class="tag warn">${season.id}</span><h3>City Under Glass</h3><p>Your bunker remains permanently. Only the accuracy board refreshes when the ${season.daysRemaining}-day season ends.</p><div class="chips"><span class="chip">${season.attempts} assessments</span><span class="chip">${season.accuracy}% accuracy</span></div>`;
  const achievements=game.progression.achievements,earned=new Set(achievements.earned);$('achievementList').innerHTML=Object.entries(achievements.definitions).map(([id,item])=>`<div class="achievement-row" style="${earned.has(id)?'':'opacity:.45'}"><div class="rank-badge">${earned.has(id)?'✓':'◇'}</div><div class="row-copy"><b>${esc(item.title)}</b><span>${esc(item.description)}</span></div><span class="row-end">+${item.components} Parts</span></div>`).join('');
  if(state.leaderboard)renderLeaderboard();
}
function renderLeaderboard(){const data=state.leaderboard;$('leaderboardList').innerHTML=data.entries.length?data.entries.map(entry=>`<div class="board-row ${entry.self?'self':''}"><div class="rank-badge">#${entry.rank}</div><div class="row-copy"><b>${esc(entry.name)} ${entry.subscriber?'◆':''}</b><span>Level ${entry.level} · ${entry.attempts} assessments</span></div><span class="row-end">${entry.accuracy}%</span></div>`).join(''):'<p>Complete an assessment to enter the 30-day accuracy board.</p>'}
async function loadLeaderboard(){try{state.leaderboard=await api('/api/game/leaderboard');renderLeaderboard()}catch(error){$('leaderboardList').innerHTML='<p>'+esc(error.message)+'</p>'}}

function renderStorage(){
  const game=state.game,inventory=game.progression.inventory;$('storageList').innerHTML=Object.entries(inventory.items).map(([id,item])=>{const owned=inventory.owned.includes(id),equipped=Object.values(game.hero.outfit).includes(id),copy=ITEM[id]||[item.name,item.effect];return `<div class="item-row" style="${owned?'':'opacity:.4'}"><div class="item-icon">${item.slot.slice(0,2).toUpperCase()}</div><div class="row-copy"><b>${esc(copy[0])}</b><span>${esc(copy[1])}</span></div><button class="secondary" data-equip="${id}" type="button" ${owned?'':'disabled'}>${equipped?'Equipped':'Equip'}</button></div>`}).join('');
  renderShop();const triggers=game.progression.conversionTriggers;$('conversionCard').classList.toggle('hidden',!triggers.length);if(triggers.length)$('conversionCard').innerHTML=`<h3>Live terminal unlocked</h3><p>${esc(triggers[0].title)} This continues the action you already learned in the Lab.</p><button class="primary full" data-live-scan type="button">Open XRadar terminal</button>`;
}
function renderShop(){if(!state.catalog){$('shopList').innerHTML='<div class="card"><p>Station support catalog is loading.</p></div>';return}$('shopList').innerHTML=state.catalog.products.map(product=>{const available=product.starsEnabled||product.tonEnabled||state.config.allowDevAuth;const price=product.starsEnabled?product.stars+' Stars':product.tonEnabled?(Number(product.tonNano)/1e9)+' TON':'Local demo';return `<div class="shop-card"><h3>${esc(product.name)}</h3><p>${esc(product.description)}</p><button data-buy="${product.id}" data-method="${product.starsEnabled?'stars':product.tonEnabled?'ton':'demo'}" type="button" ${available?'':'disabled'}>${price}</button></div>`}).join('')}
async function loadCatalog(){try{state.catalog=await api('/api/game/commerce/catalog');renderShop()}catch(error){$('shopList').innerHTML='<div class="card"><p>'+esc(error.message)+'</p></div>'}}

function renderReturn(){const report=state.game.progression.returnReport;$('returnModal').classList.toggle('hidden',!report);if(!report)return;let text='The station produced '+fmt(report.data)+' Intel during '+Number(report.hours||0).toFixed(1)+' productive hours.';if(report.full)text+=' Storage filled after '+report.capacityHours+' hours and production stopped for '+Number(report.stoppedHours||0).toFixed(1)+' hours.';if(report.streak)text+=' Login streak day '+report.streak.day+': +'+report.streak.components+' Parts'+(report.streak.passComponents?' and +'+report.streak.passComponents+' Pass Parts':'')+'.';$('returnText').textContent=text}
function resourceFly(text){const element=document.createElement('span');element.className='resource-fly';element.textContent=text;$('resourceFx').append(element);setTimeout(()=>element.remove(),900)}

function setTab(tab){state.tab=tab;qa('.tab').forEach(element=>element.classList.toggle('active',element.id==='tab-'+tab));qa('[data-tab]').forEach(element=>element.classList.toggle('active',element.dataset.tab===tab));closeSheet();tone(420);haptic();if(tab==='signals'&&!state.signalId){state.factors.clear();state.lastResult=null;renderSignals()}if(tab==='missions'&&!state.leaderboard)loadLeaderboard();if(tab==='storage'&&!state.catalog)loadCatalog()}
function setView(view){state.view=view;$('sceneMedia').className='scene-media focus-'+view;qa('[data-view]').forEach(button=>button.classList.toggle('active',button.dataset.view===view));$('sceneTitle').textContent=view==='overview'?'Full station':'Command Lab';renderUnits();tone(360)}
function openSheet(title,status,html){$('sheetTitle').textContent=title;$('sheetStatus').textContent=status;$('sheetBody').innerHTML=html;$('sheet').classList.add('open')}
function closeSheet(){$('sheet').classList.remove('open')}
function costMarkup(action){const cost=action.cost||{},reward=action.reward||{};return '<div class="chips">'+(cost.energy?'<span class="chip cost">−'+cost.energy+' Power</span>':'')+(cost.data?'<span class="chip cost">−'+fmt(cost.data)+' Intel</span>':'')+(cost.components?'<span class="chip cost">−'+cost.components+' Parts</span>':'')+(reward.data?'<span class="chip gain">+'+reward.data+' Intel</span>':'')+(reward.components?'<span class="chip gain">+'+reward.components+' Parts</span>':'')+(reward.xp?'<span class="chip gain">+'+reward.xp+' XP</span>':'')+(action.durationMs?'<span class="chip">'+duration(action.durationMs)+'</span>':'')+'</div>'}
function openObject(id){const object=state.game.objects.find(entry=>entry.id===id);if(!object)return;const copy=OBJECT[id]||[object.name,'Station system.'];let html='<p class="sheet-desc">'+esc(copy[1])+'</p>';if(id==='elevator')html+='<button class="primary sheet-action" data-view-open="overview" type="button">Open full station</button>';if(object.action){if(object.action.type==='navigate')html+='<button class="primary sheet-action" data-navigate="'+esc(object.action.target)+'" type="button">'+(object.action.target==='map'?'Open intercepted signals':'Open equipment storage')+'</button>';else if(object.action.type==='build')html+=costMarkup(object.action)+'<button class="primary sheet-action" data-build="'+esc(object.action.roomId)+'" type="button" '+(object.action.enabled?'':'disabled')+'>'+esc(object.action.label||'Build room')+'</button>';else{const action=actionCopy(object.action);html+='<p class="sheet-desc">'+esc(action[1])+'</p>'+costMarkup(object.action)+'<button class="primary sheet-action" data-action="'+esc(object.action.id)+'" type="button" '+(object.action.enabled?'':'disabled')+'>'+esc(action[0])+'</button>'}}else html+='<div class="card"><h3>System standing by</h3><p>No manual operation is currently required.</p></div>';openSheet(copy[0],object.status||'Station object',html);tone(560)}
function openRoom(id){const room=state.game.rooms[id],copy=ROOM[id];if(!room||!copy)return;let html=`<p class="sheet-desc">${esc(copy.desc)}</p><div class="card accent"><h3>Gameplay effect</h3><p>${esc(copy.effect)}</p></div>`;if(room.construction){html+=`<div class="card amber"><h3>Construction in progress</h3><p>Level ${room.construction.targetLevel} · ${duration(room.construction.remainingMs)} remaining</p><div class="progress"><i style="width:${Math.round(room.construction.progress*100)}%"></i></div></div>`}else if(!room.level&&!room.unlocked){html+=`<div class="card"><h3>Locked foundation</h3><p>${esc(room.lockReason)}</p></div>`}else if(room.nextUpgrade){html+=`<div class="card"><div class="card-top"><div><h3>${room.level?'Upgrade to level '+room.nextUpgrade.level:'Open room'}</h3><p>Maximum level is 10.</p></div><span class="tag">LV ${room.level}</span></div>${costMarkup({cost:room.nextUpgrade,durationMs:room.nextUpgrade.durationMs})}</div><button class="primary sheet-action" data-build="${id}" type="button">${room.level?'Start upgrade':'Open room'}</button>`}else html+='<div class="card"><h3>Level 10 reached</h3><p>This room is fully upgraded.</p></div>';openSheet(copy.name,room.level?'LEVEL '+room.level:room.unlocked?'FOUNDATION READY':'LOCKED',html)}
function followTask(task){if(!task)return;if(task.target==='map'){setTab('signals');return}setTab('lab');const id=({bunker:'terminal',inventory:'locker'})[task.target]||task.target;if(id==='elevator'){setView('overview');return}setView('lab');setTimeout(()=>openObject(id),260)}
async function startAction(id){try{const response=await api('/api/game/action/start',{method:'POST',body:{actionId:id}});closeSheet();setGame(response.game);tone(690);haptic('success')}catch(error){notify(error.message,true);haptic('error')}}
async function startBuild(id){try{const response=await api('/api/game/build',{method:'POST',body:{roomId:id}});closeSheet();setGame(response.game);setView('overview');tone(690);haptic('success')}catch(error){notify(error.message,true);haptic('error')}}
async function equip(id){try{const response=await api('/api/game/inventory/equip',{method:'POST',body:{itemId:id}});setGame(response.game);notify('Equipment updated and visible on the operator.');tone(640)}catch(error){notify(error.message,true)}}
async function assess(decision){const signal=state.game.progression.recon.signals.find(entry=>entry.id===state.signalId);if(!signal)return;state.lastExpected=expectedFactors(signal);try{const response=await api('/api/game/recon/resolve',{method:'POST',body:{signalId:signal.id,decision}});state.lastResult=response.result;state.game=response.game;renderAll();setTab('signals');state.lastResult=response.result;renderSignals();tone(response.result.correct?820:240,.13);haptic(response.result.correct?'success':'light');schedulePoll()}catch(error){notify(error.message,true);haptic('error')}}
async function syncLive(){try{const response=await api('/api/game/recon/sync-live',{method:'POST',body:{}});state.signalId=null;setGame(response.game);notify('Live XRadar wave loaded.')}catch(error){notify(error.message,true)}}
function openLiveScan(){const url=state.config?.xradarBaseUrl;if(url){try{if(tg?.openLink)tg.openLink(url);else window.open(url,'_blank','noopener')}catch{window.open(url,'_blank','noopener')}}else notify('Connect XRADAR_BASE_URL to enable live scans.')}
async function saveOperator(){state.editor.callSign=$('callSign').value.trim()||'Operator';try{const response=await api('/api/game/profile/appearance',{method:'POST',body:state.editor});state.editorDirty=false;state.editor={...defaultEditor,...response.appearance};localStorage.setItem(storageKey,JSON.stringify(state.editor));setGame(response.game);notify('Operator profile saved to the station.');tone(760);haptic('success')}catch(error){notify(error.message,true);haptic('error')}}
async function saveCosmetic(key,value){const cosmetics={...state.game.profile.cosmetics,[key]:value};try{const response=await api('/api/game/profile/cosmetics',{method:'POST',body:cosmetics});setGame(response.game);notify('Station appearance updated.')}catch(error){notify(error.message,true)}}
async function connectReferral(){const code=$('referralInput')?.value.trim();if(!code)return notify('Enter a referral code.',true);try{const response=await api('/api/game/referral/connect',{method:'POST',body:{code,deviceId:deviceId()}});setGame(response.game);notify('Referral connected. It qualifies after level 3 and one signal.')}catch(error){notify(error.message,true)}}
function showIncidentSheet(){const incident=state.game.progression.incidents.active;if(!incident)return;renderLab();setView('lab');tone(210,.18);setTimeout(()=>tone(260,.18),260);haptic('error');openSheet(incident.title,'TACTICAL INCIDENT',`<p class="sheet-desc">${esc(incident.description)}</p><div class="card amber"><h3>Choose a countermeasure</h3><p>Each response has a different Power cost and recovery reward.</p></div>${incident.options.map((option,index)=>`<button class="${index?'secondary':'primary'} sheet-action" data-incident-action="${option.id}" type="button">${esc(option.label)} · −${option.energy} Power · +${option.reward.data||0} Intel${option.reward.components?' · +'+option.reward.components+' Parts':''}</button>`).join('')}`)}
async function openIncident(){if(!state.game.progression.onboarding.completed){setTab('missions');notify('Complete the restoration protocol first.');return}if(state.game.progression.incidents?.active){showIncidentSheet();return}if(!state.game.progression.incidents?.ready){notify('Security systems are still analyzing the last incident.');return}try{const response=await api('/api/game/incident/start',{method:'POST',body:{}});setGame(response.game);showIncidentSheet()}catch(error){notify(error.message,true);haptic('error')}}
async function resolveIncident(action){try{const response=await api('/api/game/incident/resolve',{method:'POST',body:{action}});closeSheet();setGame(response.game);notify(response.result.message);tone(820,.14);haptic('success')}catch(error){notify(error.message,true);haptic('error')}}
function resourceInfo(type){const game=state.game,copy={intel:['Intel','Produced by the Command Lab. Automation increases offline production.','Current production: '+fmt(game.resources.productionPerHour)+'/h · offline '+fmt(game.resources.offlineProductionPerHour)+'/h · storage '+game.resources.offlineCapacityHours+'h'],power:['Power','Limits active operations and construction. The Power Room raises capacity and regeneration.','Regeneration: '+fmt(game.resources.energyRegenPerHour)+'/h'],parts:['Parts','Never produced passively. Earn them from supplies, streaks, assessments, achievements and qualified referrals.','Next supply: '+(game.progression.supply.ready?'ready now':duration(game.progression.supply.remainingMs))]};const item=copy[type];openSheet(item[0],'RESOURCE',`<p class="sheet-desc">${item[1]}</p><div class="card accent"><h3>Current rule</h3><p>${item[2]}</p></div>`)}

async function purchase(productId,method){try{const response=await api('/api/game/commerce/order',{method:'POST',body:{productId,method:method==='demo'?'stars':method,demo:method==='demo'}});if(response.demo){const completed=await api('/api/dev/commerce/fulfill',{method:'POST',body:{orderId:response.orderId}});setGame(completed.game);notify('Local demo purchase fulfilled.');return}if(method==='stars'){if(!tg?.openInvoice)throw new Error('Open the game inside Telegram to use Stars.');tg.openInvoice(response.invoiceLink,status=>{if(status==='paid')refresh().catch(()=>{});else if(status==='failed')notify('Payment failed.',true)});return}await sendTon(response.transaction,response.orderId)}catch(error){notify(error.message,true)}}
async function sendTon(transaction,orderId){try{if(!window.TON_CONNECT_UI)await loadScript('https://unpkg.com/@tonconnect/ui@latest/dist/tonconnect-ui.min.js');state.ton||=new window.TON_CONNECT_UI.TonConnectUI({manifestUrl:location.origin+'/tonconnect-manifest.json'});await state.ton.sendTransaction(transaction);notify('TON transaction sent. Verifying…');for(let attempt=0;attempt<12;attempt++){await new Promise(resolve=>setTimeout(resolve,3000));const result=await api('/api/game/commerce/ton/verify',{method:'POST',body:{orderId}});if(result.status==='paid'){setGame(result.game);notify('TON payment verified.');return}}notify('Payment sent. Verification is still pending.')}catch(error){notify(error.message,true)}}
function loadScript(src){return new Promise((resolve,reject)=>{const script=document.createElement('script');script.src=src;script.onload=resolve;script.onerror=()=>reject(new Error('Payment interface could not load.'));document.head.append(script)})}

document.addEventListener('click',event=>{
  const button=event.target.closest('button');if(!button)return;
  if(button.dataset.tab){setTab(button.dataset.tab);return}if(button.dataset.view){setView(button.dataset.view);return}if(button.dataset.viewOpen){closeSheet();setView(button.dataset.viewOpen);return}
  if(button.dataset.object){moveOperatorToObject(button.dataset.object);tone(330);setTimeout(()=>openObject(button.dataset.object),620);return}if(button.dataset.room){openRoom(button.dataset.room);return}if(button.dataset.roomFocus){setTab('lab');setView('overview');setTimeout(()=>openRoom(button.dataset.roomFocus),220);return}
  if(button.dataset.action){startAction(button.dataset.action);return}if(button.dataset.build){startBuild(button.dataset.build);return}if(button.dataset.navigate){closeSheet();setTab(button.dataset.navigate==='map'?'signals':'storage');return}
  if(button.dataset.signalOpen){state.signalId=button.dataset.signalOpen;state.factors.clear();renderSignals();tone(570);return}if(button.hasAttribute('data-signal-back')){state.signalId=null;state.factors.clear();renderSignals();return}if(button.dataset.factor){state.factors.has(button.dataset.factor)?state.factors.delete(button.dataset.factor):state.factors.add(button.dataset.factor);renderSignals();tone(470);return}if(button.dataset.assessment){assess(button.dataset.assessment);return}if(button.hasAttribute('data-result-close')){state.lastResult=null;state.signalId=null;state.factors.clear();renderSignals();return}if(button.hasAttribute('data-sync-live')){syncLive();return}if(button.hasAttribute('data-live-scan')){openLiveScan();return}
  if(button.dataset.equip){equip(button.dataset.equip);return}if(button.dataset.task){followTask(state.game.tasks.find(task=>task.id===button.dataset.task));return}if(button.dataset.incidentAction){resolveIncident(button.dataset.incidentAction);return}if(button.dataset.gender){state.editor.gender=button.dataset.gender;state.editorDirty=true;renderCrew();renderUnits();return}
  if(button.dataset.cosmeticKey){saveCosmetic(button.dataset.cosmeticKey,button.dataset.cosmeticValue);return}if(button.hasAttribute('data-referral-connect')){connectReferral();return}if(button.dataset.buy){purchase(button.dataset.buy,button.dataset.method);return}if(button.dataset.resourceInfo){resourceInfo(button.dataset.resourceInfo);return}
});
document.addEventListener('input',event=>{if(event.target.dataset.editorRange){state.editor[event.target.dataset.editorRange]=Number(event.target.value);state.editorDirty=true;renderCrew();renderUnits()}else if(event.target.id==='callSign')state.editorDirty=true});
$('saveOperator').addEventListener('click',saveOperator);$('sheetClose').addEventListener('click',closeSheet);$('goalButton').addEventListener('click',()=>followTask(state.game?.recommendedTask));$('incidentButton').addEventListener('click',openIncident);$('soundButton').addEventListener('click',()=>{setAmbient(!state.sound);tone(620)});$('levelButton').addEventListener('click',()=>setTab('crew'));
$('returnAccept').addEventListener('click',async()=>{try{const response=await api('/api/game/report/ack',{method:'POST',body:{}});setGame(response.game)}catch(error){notify(error.message,true)}});

async function enterGame(){const response=await api('/api/game');$('auth').classList.add('hidden');$('game').classList.remove('hidden');setGame(response.game);setView('lab');setAmbient(state.sound);tg?.ready();tg?.expand();loadCatalog().catch(()=>{})}
async function authenticate(body){const startParam=tg?.initDataUnsafe?.start_param||new URLSearchParams(location.search).get('ref')||'';const response=await api('/api/auth/telegram',{method:'POST',body:{...body,deviceId:deviceId(),referralCode:startParam}});if(response.referralError)notify(englishError(response.referralError,'Referral could not be connected.'),true);await enterGame()}
async function boot(){if(tg?.isVersionAtLeast?.('6.1')){tg.setHeaderColor?.('#091216');tg.setBackgroundColor?.('#05090c')}try{state.config=await api('/api/config');try{await enterGame();return}catch(error){if(error.status!==401)throw error}if(tg?.initData)await authenticate({initData:tg.initData});else if(state.config.allowDevAuth){$('authButton').classList.remove('hidden');$('authStatus').textContent='Local secure environment ready.'}else $('authStatus').textContent='Open XRadar Lab from Telegram.'}catch(error){$('authStatus').textContent=error.message}}
$('authButton').addEventListener('click',()=>authenticate({dev:true}).catch(error=>$('authStatus').textContent=error.message));document.addEventListener('visibilitychange',()=>{if(!document.hidden&&state.game)refresh().catch(()=>{})});boot();
